---
    :concurrency: 1
    :queues:
      - default
      - mailers
      - scheduler
    
    :schedule:
      scraper_webuy:
        # at 7am every day
        cron: '0 2 * * *'
        queue: scheduler
        enabled: true
        class: ScrapeLinkedin